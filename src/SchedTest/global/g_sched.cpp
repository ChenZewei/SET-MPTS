#include "g_sched.h"

GlobalSched::GlobalSched(bool LinearProgramming, uint TestMethod, uint SchedMethod, uint PriorityAssignment, uint LockingProtocol, string name, string remark): SchedTestBase(LinearProgramming, TestMethod, SchedMethod, PriorityAssignment, LockingProtocol, name, remark)
{
	
}
